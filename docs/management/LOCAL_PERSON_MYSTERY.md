# Local-person mystery development — P4-R68

Owner authorizes development using economical separate tasks. PM reuses one visible Terra Low worker sequentially; primary owns review/integration/deployment. Starting allowance85%used, mandatory stop95%used. No gameplay required for development;24h automatic-case native test remains deferred.

1. Working-key/inference foundation: pure known-fact reducer plus native helper for caller-supplied new key, actual interacted door/building matching, strict validation and tests. Worker owns KeyConnection.lua, HouseKeyAdapter.lua and focused tests/research. No automatic live mutation or new world facts in this foundation.
2. Bind anonymous story to current house and a suitable nearby existing named zombie/corpse. Persist selected name/occupation/world identity independently from player knowledge; preserve acquisition provenance through nested wallets and item movement. First reference must not leak the person's name. Inspect actual native corpse/key persistence before committing exact-once placement adapter.
3. Integrate observed match into durable journal interpretation, including reverse discovery order and duplicate/reload guards inside shared save budget. Original observations remain immutable. Match means access/association, not proven residence or authorship. Fix mixed journal ordinal presentation as part of projection integration.
4. Replace fixed clue checklist with story-driven evidence roles and variable count/carriers from audited installed catalogue. Recalculate container requirements from chosen case rather than3/4. Native-test smallest complete anonymous-clue/person/key connection, then expand templates.

These are sequential implementation segments, not completed features. Each delivery requires tested code and primary review; no claim of native acceptance from mocks. Initial object audit is docs/research/MYSTERY_OBJECT_CATALOGUE.md. A fresh save may be required for changed generated-case schema; never reset existing saves automatically.

PM checkpoint: key foundation focused tests now include independent missing/mismatched joins, physical-key mismatch, output cap and isolation, factory detachment/type/assignment failures, possession/type/default-ID/native-error guards; both pass under Lua5.1. Journal combined-view ordinals fixed in Notebook with actual projection test passing, not yet deployed. Owner requests continued work until finished. Previous foundation worker idle; fresh sole Terra Low worker01a077cc-112b-7d11-a000-ada29a2fbb96 implements variable evidence first, with person/key integration next. Native acceptance remains pending; do not claim foundation tests complete the feature.

Worker coordination: previous reused worker returned incomplete untested KeyConnection/HouseKeyAdapter drafts; PM review rejected them as insufficient for real joins/native interaction guarantees. They are not deployed. A fresh compact Terra Low task01a07797-3caf-7360-adc8-2009e2d78017 now owns replacing/completing only this foundation with tests and native API research. Previous worker remains idle; no parallel development workers. PM completed initial installed-object catalogue independently.
