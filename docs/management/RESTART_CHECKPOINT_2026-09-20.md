# Restart checkpoint — 20 September 2026

User requested saving current work because the PC may restart. This is a **work-in-progress checkpoint**, not the final Claude handoff and not a tested build.

## Where to resume

Worktree: `Conspiracy-Files`, branch `main`. The preceding committed checkpoint was `789c18b`; find this checkpoint with `git log -1`. Local main was 22 commits ahead of origin before this checkpoint. Nothing in this save operation is pushed or published. Other writing/map-media worktrees are preserved. All delegated workers had completed when this note was written.

The full active objective is the writing rebuild across all existing producers, coherent events and distinct evidence, source/survivor voice separation, grounded named businesses, fatalistic bureaucratic humour, placement variety, and complete implementation before one Linux build/testing handoff. Do not reduce completion to the currently written subset. Claude stopped; the owner authorised integration into main. Claude retains execution of tests/builds/game. **Do not execute Lua, compilers, tests, exporters or game in the implementation pass.** Fresh saves only. Plain storage, full history, provisional 1,000,000-byte aggregate allowance; no migration or compression project.

## Source work saved in this checkpoint

- `MapMediaRuntime`: kind-diverse bounded candidate pools for map fragments/payoffs; offers queue candidates rather than immediately insert. Full local scan precedes selection. Targets are re-resolved before insertion. Intent/token/reconciliation logic remains; no payoff respawn after uncertain absence.
- Destination geometry: new `MapMediaDestinations` overlays eleven previously zero-building designs with bounded source-marked areas, records provenance, and identifies a real shared Spiffo's destination for MulStashMap11/MulStashMap16. Map 16's native bank stash is untouched. Runtime supports fixed outdoor furniture and all actually loaded levels; no floor placement. Loaded target squares refine overlapping building metadata. The catalogue builder's footer matches the directly edited catalogue footer; builder was not run.
- Fault injection now scopes by design and part. Debug status exposes pending fault and a receipt with canonical state at the exact interruption. The native placement fixture includes all four interruption points plus normal insertion, isolates scheduler failure counts, requires a positive placement, distinguishes unobservable targets, and returns INCONCLUSIVE/exit 2 for missing coverage. This fixture is written, not executed.
- `MapMediaPlaceStories`: Irvington lap-time map gets a specific repair-booking dispute using the actual Cossette/Dart times. Existing 17-family registry remains; this is a place-specific repairs variant. Map content adds one shared-file finding gated on all four noted sources of both restaurant trails.
- Inventory scenarios: ten event-specific optional sources including keys, photographs and call-in transcripts; compatible source gates preserve the three-source endings. Fuel-can clue observation corrected to describe its actual key carrier.
- Identity/key/relay/retained Dead Air prose shortened and shifted toward first-person observations without claiming an identified corpse or invented survivor history.
- Draft tests updated for known-only projections, current wording, essential collection notice, optional evidence, map state furniture, real shared map pair, outdoor destination and scoped faults. Added `inventory_story_optional.lua` and `map_media_destinations.lua`. Fixed missing `contains` helper in map-content draft.

## Next work — do not claim these finished

1. Review the saved runtime and native-fixture changes carefully. In particular, total scan latency, destination availability, moved/dismantled furniture, exact interruption recovery and real multi-building resolution remain unverified; native verification belongs to Claude after implementation.
2. Review `test/case_steer.lua`: earlier worker version incorrectly compared person-substituted prose to unsteered text and demanded an organisation choice preserve the original event. Those two assertions are now corrected. Still inspect positive compatible-listen coverage, role preference guarantees, deterministic rebuild and malformed-input coverage. Do not accept a passing or vacuous draft as sufficient evidence.
3. Review `test/pile_paperwork.lua`: worker replaced obsolete generic-pile assumptions. It currently establishes physical coverage and code references, but its declared-source table must actually constrain generated optional membership, rather than only being nonempty. Preserve real authored physical contributions; do not restore arbitrary piles.
4. Complete the remaining editorial/integration review described in `WRITING_REBUILD_STATUS_2026-09-20.md` and `docs/design/WRITING_SYSTEM_AUDIT_2026-09-20.md`. The historical progress sections are snapshots; the old final checklist predates this checkpoint. All 44 generated variants and all 17 map families have source drafts, but a completed per-map editorial review is not established merely by rendering all 125 bindings. Generic recipient-copy selection still exists for unclassified maps.
5. Verify opening/continuation routing, readable places outside the address index, archive/readings/admission behavior and popup integration against the actual sources. Preserve all recorded history and the existing full-budget fixtures. Do not reimplement parts already complete.
6. Finish static review, update graph and management status, then write the **single complete Linux handoff**. Linux runs should include `tools/autotest/unit.sh`, source compilation/package/boot, revised `tools/autotest/checks/map_placement.sh`, actual loot-map/manual-read end-to-end play, fresh opening/continuation, shared restaurant pair, all reviewed outdoor destinations, placement variety, persistence and real save-size measurements. No command in this list was run by Codex.

## Evidence and limits

The last native coverage baseline remains Claude's older 105 one-building / 11 zero-building / 9 multi-building result and zero real shared destinations. New overlays change source behavior; they do not retroactively change that native evidence. Test/code edits and `git diff --check` are source work only. No tests, builds, compiler, exporter or game were executed in this checkpoint. Account usage was last read at 56% used / 44% remaining weekly; recheck before another substantial segment. Stop reserve remains 5% remaining, with no reset redemption authorised.
