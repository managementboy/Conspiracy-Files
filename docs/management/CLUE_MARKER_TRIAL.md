# Found-clue marker trial — 2026-09-05

Implements P4-R59/P4-R60 for the current generated investigation in debug single-player. ClueMarkers.lua records source squares before successful vanilla transfers into player inventory and retains the first observed source after completion. Hooks shared ISTransferAction.transferItem plus ISGrabItemAction.transferItem for floor grabs; original calls/results remain intact, marker failures are caught separately. Whole-bag transfers inspect up to 512 contained items with cycle protection. Source-less or failed transfers do not fabricate records. Pre-existing known discoveries without historical pickup records remain unmapped even if re-looted; the original assignment target and later reader position are never substituted.

Pickup captures location only. Existing generated Inspect remains the evidence discovery authority. A once-per-second worker marks eligible known clues when the character has a writing tool. Mirrors installed ISWorldMapSymbols.canWrite: recursive type/tag checks for Pen, Pencil, RedPen, BluePen, GreenPen. Bag contents qualify. No journal gating. Losing the tool pauses new annotations; reacquiring it catches up. Dropping documents/tools leaves written markers intact. Conflicted assignments cannot acquire new marks.

Per-character ModData ConspiracyFiles.ClueMarkers stores schema-1 plain original positions and written flags, bounded to 64 records/24 KB. Validate and copy before swap; combined marker/address/generated-case budget <=500 KB. Hot reload and save/reload preserve written status and backlog. No save migration or speculative historical backfill.

World map uses an independent cooperative overlay: one X per source square/floor, separate discovery numbers and titles for collocated clues; minimum zoom 14. Positions are source tile centers, not label offsets or player location. Only known, written records render. No native map-knowledge flags are modified. These are mod-owned persistent marks, not native editable/erasable annotations. Vanilla marks remain untouched. Mini-map/paper-item overlays are not included. Gameplay Help describes behavior.

Activation in an already running game: close notebook, reload media/lua/client/ConspiracyFiles/Notebook.lua, then ConspiracyFiles.NotebookUI.enableClueMarkers(). New game loads start via OnGameStart. ConspiracyFiles.ClueMarkers.status() reports marked/pending/historical-unavailable counts. Current three already-inspected documents lack historical pickup records; expect missing=3, not invented pins. Live verification requires newly found evidence (a fresh generated case/save), picking up and inspecting without a writing tool, acquiring one, removing it for subsequent evidence, then reacquiring and save/reloading. Do not erase existing journal knowledge to create a test.

Validation: test/clue_markers.lua covers source capture, original transfer behavior, floor/whole-bag pickups, unsuccessful transfer, inspection gating, tagged writing tools, catch-up idempotency, tool loss, dropped notes, historical no-backfill, reload restoration and grouped rendering. Existing suite 53/53 passes; address regression tests and Lua syntax checks pass. Native gameplay and performance remain owner acceptance gates. No native marker rendering or transfer pass is claimed from mocks.

## Offline hardening checkpoint — 2026-09-05

All active generated, address and marker writes now use SaveBudget.check and pure Validator.validateCombined, accounting for existing generated/address/marker/legacy canonical roots before replacing the staged subsystem. The 500 KB combined limit can no longer be bypassed by later generated-case writes. Marker schema rejects extra fields; transfer completion rechecks assignment identity/conflict. Corrupt state and failing storage do not cancel the original vanilla inventory transfer. Notebook MAP NOTE explains marked/pending/historical-unavailable status without changing evidence text. Manual Trial.start starts source capture before case generation; loading the module is passive.

Test outputs are stored in evidence/2026-09-05-offline. All seven test groups pass, including 53 core tests, native-adapter mocks, budget boundary tests and startup ordering. Seven live Lua files installed with SHA-256 manifest and backup 20260905-210605-offline-checkpoint. No live game input or save manipulation was performed. Full restart required for this checkpoint; follow TOMORROW_PLAYTEST.md instead of earlier hot-load commands. Live acceptance remains pending.

## Economical development batch — 2026-09-05

P4-R61 is active: one short-context Terra Low worker handles independent marker-label viewport work; primary handles integration and review. Primary session optimization makes repeated placed/unknown/conflict status reports no-ops after existing transition checks. This avoids whole-case serialization/replacement during unchanged identity scans; it does not weaken sticky conflicts or permit placement resets. Regression exercises 120 repeated status/Inspect calls with zero additional sink writes and stable saved-root identity. Core suite 53/53 and G2 runtime mocks pass. Native timing is not inferred from this optimization.

Integration review completed: scoped Terra Low worker implemented pure MarkerLayout and rendering adapter changes; primary requested fixes for real-font width and UTF-8 truncation, then verified the corrected helper and regenerated the Notebook bundle. Labels stay inside viewport bounds, shorten long titles, preserve clue numbers/floor where space permits, and leave X at the original source position. Crowded groups display only rows that fit; different source groups are not globally deconflicted. Focused layout and marker tests pass; changed-file syntax passes. No live validation performed. Existing state and numbering remain unchanged.
