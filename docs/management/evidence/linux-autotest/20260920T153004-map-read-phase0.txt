Linux map-read Phase 0 check 20260920T153004: PASS (mechanism proven; NOT player-path acceptance)
source: 25b5a0a (branch codex/map-media)
game: 42.20.4 b0bbce05d5
renderer: Intel Mesa Intel(R) Iris(R) Xe Graphics (RPL-U) on display :0

QUESTION (map plan Phase 0): can the engine tell us that a map was read, and
WHICH map? A generic "a map was used" is not enough - without the design
identity there is no destination.

ANSWER: yes.

Static source evidence, installed game:
  ISInventoryPaneContextMenu.onCheckMap = function(map, player)   line 2661
    - the reader receives the map ITEM, so identity is reachable.
  StashUtil.newStash("BBurgStashMap1", "Map", "Base.RosewoodMap", "Stash_AnnotedMap")
    - the 125 catalogue ids are STASH names, not item types. Many designs share
      one item type (Base.RosewoodMap), so item type alone cannot identify a
      design. This is why an item-type lookup would have been wrong.

Live evidence, this session:
  - Base.RosewoodMap exposes getStashMap() and setStashMap().
  - getStashMap() returned "BBurgStashMap1" - exactly the vocabulary the mod's
    catalogue uses, so no translation layer is needed.
  - Driving the game's own onCheckMap produced 5 observer rows, 4 carrying the
    design identity BBurgStashMap1.
  - The mod reads it via item:getStashMap() (colon form, per the engine call rule).

WHAT THIS DOES NOT ESTABLISH, and must not be reported as passed:
  - The map was created with setStashMap and the read was driven through the
    game's function by the test channel. It was NOT acquired from ordinary loot
    and NOT opened by a human clicking the context menu. Acquisition-through-loot
    and the real player path remain unverified (map plan Phase 0 asks for both).
  - No placement, no fragment offer, no destination evidence was exercised here.
  - Nothing about hook order against other mods, or cost.
