# Live test of the day's features — 2026-09-25

`/goal live test our new features. Fix any found errors` — the occupation
openings (24 new families), the opening memory, the THREADS redesign and the
key-door voice, all on the real Linux game.

## What was run, and what it found

| Run | Result | What it proved |
|---|---|---|
| `profession_openings` all 25 families, 1 fresh save each (`20260925T193704` second run) | **PASS** | every family routes, its own start title, the pocket object on the player at once, memory read before each save |
| `fitness_mystery_audit` as **electrician** (`20260925T195450`) | **PASS** | bound to the cordon pair; ham radio and 11-item kit materialise on arrival; every object carries the story |
| `fitness_mystery_audit` as **nurse** (`20260925T195855`) | **PASS** | bound to the farm pair; key in hand; feed sack and 9 masks materialise on arrival |
| `knox` (`20260925T200321`) | **PASS** | THREADS on the unemployed opening |
| `reload` (`20260925T202233`) | **PASS** | |
| `core_loop` on the unemployed opening | FAIL ×5, then **PASS** (`20260925T214359`) | see below |

## Errors found and fixed

**Product**

1. **A transport scene reality never promoted held the case open.** Every
   opening's fifth clue waits for a *confirmed* vanilla vehicle near the
   second site; most saves have none, so with every essential found the case
   could not complete for three in-game days and "What do I make of it?"
   never fired. The Fitness audits had all seen the cooler wait and called it
   "not exercised". Now a waiting vehicle clue is set aside at completion,
   listed among the clues the case never had. DR-20260925-SCENE-AT-COMPLETION.
2. **After any profession opening, no second case ever came.** The follow-up
   was refused as `continuation company mismatch` (placeholder company vs the
   thread's), deferred, and retried into the same refusal. The Fitness ten had
   this too; no check had ever followed a profession opening past its first
   case. The continuation now keeps the company it follows.
   DR-20260925-FOLLOW-UP-KEEPS-COMPANY.

**Harness** (each named with the run that showed it wrong)

- `profession_openings` drew a fresh world only for saves 2..N of each family,
  so with one save per family every family after the first "played" the first
  family's case (`20260925T193704` first run).
- `core_loop` looked for `DATE NOTE`; the heading is the survivor's own since
  DR-20260925-RECORD-VOICE.
- `core_loop` counted a never-promoted scene as a found document and a map
  mark; it is counted apart. It also waited for the poller's tick after
  removing the gap — 600 ticks at about one frame a second on the unfocused
  display — and now asks the poller directly.
- `fitness_mystery_audit` takes `CF_PROFESSION` and audits any occupation.

## Not exercised

- The vehicle placement path itself (no confirmed vanilla scene was near any
  site in any run today); the audits name the wait.
- The other 22 families past their first clue: only nurse and electrician were
  audited to the second site; only unemployed was played to completion.
