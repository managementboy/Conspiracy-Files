# Offline baseline at `dc73e1c` — task 1

One revision, one machine, one run. Everything below was executed; nothing is
carried forward from an earlier report.

**Revision:** `dc73e1c8b89fd1a3911d6f5401909bd939e42a87` (`main`, no tag at HEAD).
**Working tree:** clean before the run; the only untracked file afterwards is
this task's own evidence file.
**Evidence:** `evidence/linux-autotest/20260921T123912-baseline-offline.txt`.

## Environment

| | |
|---|---|
| Repository | `/home/elkin/Conspiracy-Files` (the nested Git repository; `git rev-parse --show-toplevel` agrees) |
| Lua | `Lua 5.1.5` at `/usr/bin/lua5.1` |
| Java | `openjdk 17.0.20` |
| Kahlua runner | `tools/kahlua/run.sh` with `RunLua.class` present |
| Project Zomboid | installed at `~/.steam/steam/steamapps/common/ProjectZomboid` |
| Renderer | not applicable — no game was started for this baseline |
| Tags in repo | `DEV-0.45.0-map-media`, `DEV-0.45.1-map-media`, `DEV-0.46.0-writing-rebuild`, `DEV-0.46.1-writing-rebuild` — none of them points at HEAD |

The published Workshop build is `DEV-0.46.1-writing-rebuild` at `e431d82`.
HEAD is nine commits ahead of it, so **the baseline below is not the published
build**; it is the candidate line.

## What was run

```bash
bash tools/autotest/unit.sh
python3 -m unittest discover -s test -p '*_test.py'
```

`unit.sh` is the whole offline tier: it runs the Kahlua parse-all check, then
`test/run.lua` (the six `*_spec.lua` files), then **every** other `test/*.lua`
on its own, then `tools/autotest/checks/relocation_evidence_test.sh`.

| Stage | Result |
|---|---|
| Kahlua parse of every shipped Lua file | **PASS** — 124 ok, 0 failed |
| Spec runner (`test/run.lua`) | **PASS** — 52 tests, 0 failures |
| Standalone `test/*.lua` | **PASS** — 172 run, 0 failed |
| `relocation_evidence_test.sh` | **PASS** |
| `test/package_experimental_test.py` | **PASS** — 3 tests |

The 172 reconciles exactly: `ls test/*.lua` is 179 files, minus the six
`*_spec.lua` files and `run.lua`, which the runner excludes because
`test/run.lua` owns them. The specs' 52 assertions are counted inside that 172
as one entry, which is why the script's own line says "including specs".

## Failure catalogue

**There are none offline.** Every category below is therefore empty, and that
is the finding:

| Category | Count |
|---|---|
| Shipped product | 0 |
| Unshipped prototype | 0 |
| Stale fixture | 0 |
| Environment / tooling | 0 |
| Unknown | 0 |

## What this baseline does *not* say

This is the part the previous reports got wrong, so it is stated plainly.

1. **The campaign gate is NOT EXERCISED at this SHA.**
   `tools/autotest/checks/campaign.sh` is not part of `suite.sh` and was not
   run here. The most recent campaign evidence
   (`20260921T111236-campaign.txt`) is from an older revision and is a FAIL.
2. **Every native gate is NOT EXERCISED at this SHA.** No game was started.
3. **Nine of the 172 "offline" tests are prototype tests, not shipped tests.**
   They load `dev/next-phase/?.lua` onto `package.path`:
   `campaign_policy`, `draft_cases`, `evidence_archive`, `first_clue`,
   `investigation_flow`, `location_readiness`, `multi_case_record`,
   `object_bookmarks`, `perf_stats`. They pass today, but while they sit in the
   same command as the shipped suite an unshipped prototype can turn the
   shipped baseline red. Task 2 separates them.
4. **`test/package_experimental_test.py` is unreachable from `unit.sh`.** It is
   a real test of the packaging tool and nothing runs it. Task 2 gives it a
   command.
5. **`dev/t10-cooperative-inspect/test/static_probe_test.lua` is unreachable**
   from any documented command. It belongs to a closed spike.
6. Kahlua parse-all proves the game's compiler accepts the 124 shipped files.
   It does not execute them, and it says nothing about the 11 files under
   `dev/next-phase/`, which are not shipped and not parsed.

## Verdict

**The offline baseline is green, and the product is not.** Calling the build
validated would require the campaign gate and the native gates, neither of
which has been run at this revision. The correct description of `dc73e1c`
today is *offline-green, natively unvalidated*.

No production file was changed in this task.
