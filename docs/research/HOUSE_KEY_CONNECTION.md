# House-key connection foundation — Build 42.20.4

PM source check: installed `media/lua/shared/TimedActions/ISOpenCloseDoor.lua` performs native `self.item:ToggleDoor(self.character)` in `complete()`, returning true; `perform()` only advances the timed-action queue. Any future cooperative observation wrapper must preserve the original completion call and return, and must not itself trigger an additional action. An observed matching key is not proof of an unlock. `ISLockDoor.lua` uses `self.door`/`self.character` and mutates locks in `complete()`; its vanilla validation may initialise IDs through `checkKeyId`, which our adapter never calls. Possession inside a key ring still requires verified integration.

Installed `media/lua/shared/Items/SpawnItems.lua` lines175 onward creates identity documents and calls `card:nameAfterDescriptor(playerObj:getDescriptor())`. Existing descriptor names can therefore be used for explicitly authored new identity evidence, without renaming existing cards or treating a hidden descriptor as player knowledge. This source check is not native acceptance of corpse placement.

Installed vanilla source was inspected on 2026-09-06. `DebugUIs/DebugContextMenu.lua` creates a detached `Base.Key1` with `InventoryItemFactory.CreateItem`, then assigns an existing building definition key ID with `setKeyId`. It also uses `IsoDoor:checkKeyId()` while preparing debug keys; that method may initialise a missing door key ID, so this foundation never calls it.

`ISWorldObjectContextMenu.lua` and `ISLockDoor.lua` likewise demonstrate the native key-ID APIs, but this module deliberately does not invoke lock/unlock actions. `HouseKeyAdapter.createForBuilding(building)` is explicit, creates one unattached key and only accepts an existing non-negative `building:getDef():getKeyId()`. It does not add the item to a container or alter a building, door, lock or existing item.

`observeInteractedMatch(context)` accepts only a caller-supplied actual door interaction, exact held key (checked with the player's inventory `contains`), non-default equal key IDs, and the door square's exact building definition ID. It neither scans doors/keys nor calls native actions; success is solely a `keyDoorMatch` observation, never an assertion that the door unlocked.

`KeyConnection` is PZ-free and capped at 64 records for each observation kind. A connection is derived only if an anonymous clue and the matched door name the same building, the observed key matches the door key ID, and a named document and key share the same source token. It reports possible access/association only, not residence, ownership or authorship. Inputs and outputs are copied; duplicate replay is inert and contradictory same-kind IDs are refused.

Focused Lua mocks verify creation guards, no load-time work, possession/interaction/building/key guards, and that `checkKeyId` remains untouched. These mocks establish no live-game acceptance: a manual debug Build 42 test still needs to create a detached key for an existing building, hold it, interact with a real door, and confirm the emitted observation without changing a lock or door.

Verification 2026-09-06: PUC Lua 5.1.5 syntax-checked the two real mod modules and both focused tests, then ran `test/key_connection.lua` and `test/house_key_adapter.lua` successfully. The tests load `mod/common/media/lua/...` directly and cover ordered/reversed/replayed observations, malformed/corrupt/metatable inputs, caps, copy isolation, distinct physical-key tokens with the same lock ID, inert module load, detached creation, type/possession/context guards, and thrown native getters. Live Build 42 confirmation remains required.
