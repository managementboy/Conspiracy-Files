# OWNER REVIEW REQUIRED — next-phase grounded draft cases

`dev/next-phase/DraftCases.lua` is an offline drafting exercise, not approved game text and not a live content integration. It constructs two small ambiguous record sets from one supplied fact table: a stock discrepancy and inconsistent maintenance readings. Each has three documents across two sites and preserves conflicting observations as attributed records; neither explains the Knox Event or supplies an authoritative conspiracy answer.

The drafts use stable seed selection and repeat the same names, organisation, code, and chronology through their documents. `project` returns only explicitly supplied discovered documents and hides links to undiscovered targets. The module is intentionally separate from `Generated/Generator.lua`, offers no content platform, and must not be integrated or given a tonal direction until owner review.
