ConspiracyFiles=ConspiracyFiles or {}
ConspiracyFiles.T11Mode=true
-- Loaded before OnGameStart. Production adapters run with a distinct canonical
-- tag and only D1 eligible. Never enable alongside the T12 wrapper.
