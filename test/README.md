# Tests

Automated tests, fixtures, deterministic model tests, schema tests, migration tests, and documented live-game verification scenarios belong here.

Anything requiring proprietary Project Zomboid runtime classes may need in-game verification rather than ordinary unit tests; those boundaries should be documented explicitly.
